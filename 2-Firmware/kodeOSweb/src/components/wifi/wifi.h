#ifndef WIFI_H
#define WIFI_H

#include "esp_err.h"  // Incluye esp_err.h para definir esp_err_t

// Declaración de las funciones para inicializar el Wi-Fi y la tarjeta SD
void wifi_init();

#endif // WIFI_H
